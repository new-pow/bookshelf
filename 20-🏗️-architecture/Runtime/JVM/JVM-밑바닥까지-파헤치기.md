# 📕 JVM 밑바닥까지 파헤치기
- 읽은 기간 : 2026. 03. 02 ~ 
---
# 1장.  자바 기술 시스템 소개
- 자바의 장점
	- 엄격한 구조를 갖춘 객체지향 프로그램 언어
	- 하드웨어 플랫폼에서 자유로워져, 어디서든 실행 가능
	- 안전한 메모리 관리시스템 -> 누수관리, 메모리 포인터 안정성
	- 런타임에 핫 코드 감지. 컴파일하고 최적화하여 자바 애플리케이션 최상의 성능을 내도록 도와줌
	- 표준 API 가 풍부하고, 기엄과 오픈 소스 커뮤니티에서 제공하는 다양한 기능
- ==? 이정도는 이제 다른언어에서도 강점이 있지 않을까? 자바를 사용하는 이유가 또 뭐가 있을까?==
---
# 2장.  자바 메모리 영역과 메모리 오버플로
## 2.2 런타임 데이터 영역
- 자바 프로그램을 실행하는 동안 필요한 메모리를 몇 개의 데이터 영역으로 나눠 관리
- 각 영역들은 목적/생성시점/삭제시점이 있다.

| 영역      | 목적            | 생성시점 | 삭제시점 | 생성단위         |
| ------- | ------------- | ---- | ---- | ------------ |
| 프로그램카운터 | 프로그램 제어 흐름 관리 |      |      | 스레드 프라이빗 메모리 |

![](https://github.com/user-attachments/assets/dc891134-17b9-47ed-849c-26222613e63e)

### 프로그램 카운터
- 현재 실행중인 스레드의 바이트코드 줄 번호 표시기
- 프로그램 제어 흐름, 분기, 순환, 점프 등을표현한다.
- JVM에서 멀티스레딩은 CPU 코어를 여러 스레드가 교대로 사용하는 방식으로 구현되어있다.
	- 특정 시각에 각 코어는 한 스레드의 명령어만 실행하게 된다.
	- 즉, 스레드 전환 후 이전에 실행하다 멈춘 지점을 정확하게 복원하려면 각 스레드는 고유한 프로그램 카운터가 필요하다.
- 스레드 프라이빗메모리
	- 스레드카운터는 서로 영향을 주지 않는 독립된 영역에 저장된다.
- 